import React from 'react';
import './About.css';

import Pane from '../Pane';

const About = ({ users, ...props }) => {
  return (
    <div className="About">
    </div>
  )
};

export default About;
